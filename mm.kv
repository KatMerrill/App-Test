#:kivy 1.0.9
# note that the above identifies the current version of kivy, and the file name refers to "__App" in main.py

# defines a widget
<HomePage>:    
    # canvas:
    Button:
        pos: 0, root.height - 50
        size: root.width, 50
        # background_color works as a tint rather than a fill, so using background_normal to set the background to white
        background_normal: ''
        background_color: 0, 0.533, 0.412, 0.8
    
    # Label:
    #     font_size: 70  
    #     center_x: root.width / 4
    #     top: root.top - 50
    #     text: "0"
        
    # Label:
    #     font_size: 70  
    #     center_x: root.width * 3 / 4
    #     top: root.top - 50
    #     text: "0"