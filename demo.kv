#:kivy 2.1.0

MDBoxLayout:
    orientation: "vertical"
    MDTopAppBar:
        title: "Multicast Mobile"
        screen_manager: screen_manager
        left_action_items:
            [
            ["home", lambda x: app.callback(self, screen_manager, "Home"), "Home"]
            ]
        right_action_items:
            [
            ["camera", lambda x: app.callback(self, screen_manager, "Record"), "Record"],
            ["account", lambda x: app.callback(self, screen_manager, "Account"), "Account"]
            ]
    
    ScreenManager:
        id: screen_manager
        Screen:
            name: "Home"
            id: home
              

        Screen:
            name: "Search"                   
            MDLabel:
                text: " search button"
                halign: "center" 
        Screen:
            name: "Record"
            FloatLayout:
                Label:
                    text: "Webcam from OpenCV?"
                    pos_hint: {"x":0.0, "y":0.5}
                    size_hint: 1.0, 0.2

                ### Uncomment this if on a Linux device
                # Camera:
                #     id: camera
                #     resolution: (1920, 1080)
                #     play: True

                MDRaisedButton:
                    id: button
                    text: 'Start Video'
                    pos_hint: {"x":0.1, "y":0.05}
                    size_hint: 0.8, 0.15
                    font_size: self.width/20
                    background_normal: ""
                    background_color: "#12a8ff"
                    on_release: app.startstop()
                    

        Screen:
            name: "Account"                   
            Image:
                source: "info_panel.jpg"
                pos_hint: {'y' : -0.02}
                


        Screen:
            name: "Watch"
            # MDLabel:
            #     text: "Currently playing:"
            #     pos_hint: {'x' : 0.2, 'y' : 0.8}

            Video:
                id: stream_player


<Thumbnail>:
    background_normal: "default.png"
    